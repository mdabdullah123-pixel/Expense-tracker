# Expense Categorization Tasks

- [ ] Update `AIService` with `categorize_expense()` implementation.
- [ ] Add suggestion UI to `pages/expenses.py`.
- [ ] Add validation for allowed expense categories.
- [ ] Create unit tests for AI categorization and suggestion flow.
- [ ] Document feature in `USER_MANUAL.md` and `README.md`.
