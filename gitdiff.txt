diff --git a/pages/expenses.py b/pages/expenses.py
index 7037c2a..bef84ec 100644
--- a/pages/expenses.py
+++ b/pages/expenses.py
@@ -5,14 +5,14 @@ Allows users to add, view, edit, and delete expenses and income records.
 Includes AI-powered category suggestions and receipt scanning.
 """

-import streamlit as st
 import logging
 from datetime import date, datetime
 from typing import Optional

-from database.repository import ExpenseRepository, IncomeRepository
+import streamlit as st
+
 from database.models import Expense, Income
-from services.analytics_service import AnalyticsService
+from database.repository import ExpenseRepository, IncomeRepository
 from utils.helpers import format_currency, validate_amount
 from utils.receipt_parser import ReceiptParser

@@ -27,10 +27,10 @@ def render_expenses():

     # Tab layout for different operations
     tab1, tab2, tab3, tab4 = st.tabs([
-        "GPò Add Expense",
-        "=ƒÆ¦ Add Income",
-        "=ƒôï View All",
-        "=ƒô+ Scan Receipt",
+        "Add Expense",
+        "Add Income",
+        "View All",
+        "Scan Receipt",
     ])

     with tab1:
@@ -205,7 +205,7 @@ def render_view_all():
     # Filters
     col1, col2 = st.columns(2)
     with col1:
-        filter_type = st.selectbox(
+        st.selectbox(
             "Filter by type",
             options=["All", "Expenses", "Income"],
         )
@@ -236,12 +236,10 @@ def render_view_all():
                 with cols[3]:
                     st.markdown(f"{expense.description}")
                 with cols[4]:
-                    # Delete button
                     delete_key = f"del_exp_{expense.id}"
-                    if st.button("=ƒùæn+Å", key=delete_key, help="Delete this expense"):
-                        if ExpenseRepository.delete_expense(expense.id):
-                            st.success("Deleted!")
-                            st.rerun()
+                    if st.button("=ƒùæn+Å", key=delete_key, help="Delete this expense") and ExpenseRepository.delete_expense(expense.id):
+                        st.success("Deleted!")
+                        st.rerun()
                 st.divider()
     else:
         st.info("No expenses recorded yet.")
@@ -271,10 +269,9 @@ def render_view_all():
                     st.markdown(f"{income.notes or ''}")
                 with cols[4]:
                     delete_key = f"del_inc_{income.id}"
-                    if st.button("=ƒùæn+Å", key=delete_key, help="Delete this income"):
-                        if IncomeRepository.delete_income(income.id):
-                            st.success("Deleted!")
-                            st.rerun()
+                    if st.button("=ƒùæn+Å", key=delete_key, help="Delete this income") and IncomeRepository.delete_income(income.id):
+                        st.success("Deleted!")
+                        st.rerun()
                 st.divider()
     else:
         st.info("No income recorded yet.")
@@ -404,7 +401,7 @@ def render_receipt_scanner():
                     )


-def parse_date(date_str: Optional[str]) -> Optional[date]:
+def parse_date(date_str: str | None) -> date | None:
     """Try to parse a date string into a date object."""
     if not date_str:
         return None
diff --git a/sample_data.py b/sample_data.py
index c07f8e2..70e4857 100644
--- a/sample_data.py
+++ b/sample_data.py
@@ -8,10 +8,10 @@ Run this script to populate the database with realistic sample data.
 import logging
 from datetime import date, timedelta
 from random import choice, randint, uniform, seed
+import logging

 from database.db import init_db
 from database.repository import ExpenseRepository, IncomeRepository
-from database.models import Expense, Income

 logger = logging.getLogger(__name__)

@@ -127,7 +127,7 @@ SAMPLE_EXPENSES = {
     ],
 }

-SAMPLE_INCOME = [
+SAMPLE_INCOME: list[dict[str, int | str]] = [
     {"source": "Salary", "min": 40000, "max": 80000},
     {"source": "Freelance", "min": 5000, "max": 25000},
     {"source": "Investment", "min": 1000, "max": 10000},
@@ -177,9 +177,7 @@ def generate_sample_data(num_expenses: int = 100, num_income: int = 12):
             amount = round(uniform(20, 2000), 2)
         elif category == "Shopping":
             amount = round(uniform(200, 15000), 2)
-        elif category == "Entertainment":
-            amount = round(uniform(100, 5000), 2)
-        elif category == "Health":
+        elif category in ("Entertainment", "Health"):
             amount = round(uniform(100, 5000), 2)
         elif category == "Education":
             amount = round(uniform(200, 10000), 2)
diff --git a/services/analytics_service.py b/services/analytics_service.py
index cbb30e4..816c946 100644
--- a/services/analytics_service.py
+++ b/services/analytics_service.py
@@ -6,13 +6,13 @@ and generating data contexts for AI-powered analysis.
 All calculations use pandas for efficient data processing.
 """

+from datetime import date, timedelta
 import logging
-from datetime import date, datetime, timedelta
-from typing import List, Optional
+from typing import Any, Optional
+
 import pandas as pd

 from database.repository import ExpenseRepository, IncomeRepository
-from database.models import Expense, Income

 logger = logging.getLogger(__name__)

@@ -41,7 +41,7 @@ class AnalyticsService:
         return IncomeRepository.get_total_income() - ExpenseRepository.get_total_expenses()

     @staticmethod
-    def get_expenses_dataframe(start_date: Optional[date] = None, end_date: Optional[date] = None) -> pd.DataFrame:
+    def get_expenses_dataframe(start_date: date | None = None, end_date: date | None = None) -> pd.DataFrame:
         """
         Get expenses as a pandas DataFrame for analysis.

@@ -75,7 +75,7 @@ class AnalyticsService:
         return pd.DataFrame(data)

     @staticmethod
-    def get_income_dataframe(start_date: Optional[date] = None, end_date: Optional[date] = None) -> pd.DataFrame:
+    def get_income_dataframe(start_date: date | None = None, end_date: date | None = None) -> pd.DataFrame:
         """
         Get income as a pandas DataFrame for analysis.

@@ -107,7 +107,7 @@ class AnalyticsService:
         return pd.DataFrame(data)

     @staticmethod
-    def get_monthly_spending(year: Optional[int] = None) -> pd.DataFrame:
+    def get_monthly_spending(year: int | None = None) -> pd.DataFrame:
         """
         Get monthly spending breakdown.

@@ -133,7 +133,7 @@ class AnalyticsService:
         return monthly.sort_values("month")

     @staticmethod
-    def get_monthly_income(year: Optional[int] = None) -> pd.DataFrame:
+    def get_monthly_income(year: int | None = None) -> pd.DataFrame:
         """
         Get monthly income breakdown.

@@ -159,7 +159,7 @@ class AnalyticsService:
         return monthly.sort_values("month")

     @staticmethod
-    def get_category_breakdown(start_date: Optional[date] = None, end_date: Optional[date] = None) -> dict:
+    def get_category_breakdown(start_date: date | None = None, end_date: date | None = None) -> dict:
         """
         Get spending breakdown by category.

@@ -174,7 +174,7 @@ class AnalyticsService:
         return {cat: amt for cat, amt in totals}

     @staticmethod
-    def get_top_categories(limit: int = 5) -> List[tuple]:
+    def get_top_categories(limit: int = 5) -> list[tuple[str, float]]:
         """
         Get top spending categories.

@@ -285,7 +285,7 @@ class AnalyticsService:
         return "\n".join(context_parts)

     @staticmethod
-    def detect_unusual_spending() -> List[dict]:
+    def detect_unusual_spending() -> list[dict[str, Any]]:
         """
         Detect potentially unusual spending patterns.

@@ -305,7 +305,7 @@ class AnalyticsService:
         three_months_ago = current_month_start - timedelta(days=90)
         historical_df = AnalyticsService.get_expenses_dataframe(three_months_ago, current_month_start)

-        unusual = []
+        unusual: list[dict[str, Any]] = []

         if current_df.empty or historical_df.empty:
             return unusual
@@ -318,6 +318,7 @@ class AnalyticsService:
         historical_monthly = historical_df.groupby(["year_month", "category"])["amount"].sum().reset_index()
         historical_avg = historical_monthly.groupby("category")["amount"].mean()

+        unusual: list[dict[str, Any]] = []
         for cat in current_cat.index:
             if cat in historical_avg.index and historical_avg[cat] > 0:
                 current_amt = current_cat[cat]
diff --git a/utils/receipt_parser.py b/utils/receipt_parser.py
index 8b50cb7..38da8fd 100644
--- a/utils/receipt_parser.py
+++ b/utils/receipt_parser.py
@@ -6,12 +6,13 @@ Processes extracted text to identify merchant names, dates, amounts, and line it
 Supports common receipt formats and falls back to AI-based parsing when needed.
 """

+from datetime import datetime
+import contextlib
+import io
 import logging
 import re
-from datetime import datetime
-from typing import Optional
+
 from PIL import Image
-import io

 logger = logging.getLogger(__name__)

@@ -25,7 +26,7 @@ class ReceiptParser:
     3. Identifies merchant, date, total, and items from text
     """

-    def __init__(self, tesseract_cmd: Optional[str] = None):
+    def __init__(self, tesseract_cmd: str | None = None):
         """
         Initialize the receipt parser.

@@ -33,7 +34,7 @@ class ReceiptParser:
             tesseract_cmd: Path to tesseract executable (optional)
         """
         self.tesseract_cmd = tesseract_cmd
-        self._tesseract_available = None
+        self._tesseract_available: bool | None = None

     def _check_tesseract(self) -> bool:
         """
@@ -62,7 +63,7 @@ class ReceiptParser:
             self._tesseract_available = False
             return False

-    def extract_text_from_image(self, image_bytes: bytes) -> Optional[str]:
+    def extract_text_from_image(self, image_bytes: bytes) -> str | None:
         """
         Extract text from a receipt image using OCR.

@@ -134,7 +135,7 @@ class ReceiptParser:
         logger.info("Receipt parsed: merchant=%s, total=%s", result["merchant"], result["total"])
         return result

-    def _extract_merchant(self, text: str) -> Optional[str]:
+    def _extract_merchant(self, text: str) -> str | None:
         """
         Extract merchant/store name from receipt text.

@@ -151,15 +152,18 @@ class ReceiptParser:
         # Take first non-empty line as merchant
         for line in lines:
             line = line.strip()
-            if line and len(line) > 2 and len(line) < 100:
-                # Skip lines that look like dates, addresses, or totals
-                if not re.search(r"\d{2}[/\-\.]\d{2}[/\-\.]\d{2,4}", line):
-                    if not re.search(r"total|amount|bill|receipt|tax|gst", line.lower()):
-                        return line.strip()
+            if (
+                line
+                and len(line) > 2
+                and len(line) < 100
+                and not re.search(r"\d{2}[/\-\.]\d{2}[/\-\.]\d{2,4}", line)
+                and not re.search(r"total|amount|bill|receipt|tax|gst", line.lower())
+            ):
+                return line.strip()

         return None

-    def _extract_date(self, text: str) -> Optional[str]:
+    def _extract_date(self, text: str) -> str | None:
         """
         Extract date from receipt text.

@@ -197,7 +201,7 @@ class ReceiptParser:

         return None

-    def _extract_total(self, text: str) -> Optional[float]:
+    def _extract_total(self, text: str) -> float | None:
         """
         Extract total amount from receipt text.

@@ -218,10 +222,8 @@ class ReceiptParser:
         for pattern in patterns:
             matches = re.findall(pattern, text, re.IGNORECASE)
             for m in matches:
-                try:
+                with contextlib.suppress(ValueError):
                     amounts.append(float(m))
-                except ValueError:
-                    pass

         if amounts:
             # Return the largest amount (usually the total)
@@ -315,6 +317,6 @@ class ReceiptParser:
                 scores[category] = score

         if scores:
-            return max(scores, key=scores.get)
+            return max(scores, key=lambda x: scores[x])

         return "Other"
\ No newline at end of file
